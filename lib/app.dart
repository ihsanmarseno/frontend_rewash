import 'package:flutter/material.dart';
import 'package:frontend_rewash/app_view.dart';

class MyAppHome extends StatelessWidget {
  const MyAppHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const MyAppView();
  }
}
